// amount of space algorithm takes as input grow
function reverse(arr){
    let rev = []

    for(let i = arr.length-1; i>=0; i--){
        rev.push(arr[i])
    }
}
// S .c -> O(n)



let q = 2
let b = 3
let c = 5

// s .p = O(1)

// their is ecormmerse company which asked two enginerr's to design a 
// searching algo
// company have inventory of 10^16 products.
// both the enginner come up with algo first engg algo
// take O(n) to sarch  and other's algo take O(log2n) to serch 
// if comput excute 10^8 operations/sec so how much time both algorithm will take


let arr = [1,2,3,4]
let target = 3
for(let i = 0; i < arr.length;i++){
    if(arr[i] == target){
        console.log(i)
        break;
    }
}

// Time Complexity -> O(n)
// Space Complexuty -> O(1)

